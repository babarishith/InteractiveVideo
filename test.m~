im = imread('pen.jpg');
I = im2bw(im);
e = edge(I, 'Sobel');
s = strel('disk', 50);
d = 1-imopen(I, s);
se = strel('disk', 25);
d = imdilate(d, se);
nb = imclearborder(d, 4);
m = uint8(repmat(nb, [1 1 3]));
I1 = rgb2gray(im);
imshow(m.*im);


